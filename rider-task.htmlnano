<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rider Dashboard</title>
    <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore-compat.js"></script>
    <style>
        body { font-family: sans-serif; background: #f0f2f5; padding: 20px; }
        .card { background: white; padding: 15px; border-radius: 10px; margin-bottom: 10px; border-left: 5px solid #2e7d32; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
        button { background: #2e7d32; color: white; border: none; padding: 12px; border-radius: 5px; width: 100%; font-weight: bold; cursor: pointer; }
    </style>
</head>
<body>
    <h2 style="text-align:center;">🚴‍♂️ Rider Task List</h2>
    <div id="list">အော်ဒါရှာနေသည်...</div>

    <script>
        const pass = prompt("Password:");
        if(pass !== "1234") {
            location.href="index.html";
        }

        const config = {
            apiKey: "AIzaSyD1KAKqhFVi8VTJRtoolzQKrEmXTnmZUsc",
            authDomain: "ah-mar-thaw.firebaseapp.com",
            projectId: "ah-mar-thaw",
            storageBucket: "ah-mar-thaw.appspot.com",
            messagingSenderId: "33857508316",
            appId: "1:33857508316:web:865529f7f9859f7c07b469"
        };
        firebase.initializeApp(config);
        const db = firebase.firestore();

        db.collection("orders").orderBy("orderTime", "desc").onSnapshot(snapshot => {
            let html = "";
            snapshot.forEach(doc => {
                const o = doc.data();
                if (o.status !== "ပို့ဆောင်ပြီး") {
                    html += `
                    <div class="card">
                        <b>📦 ${o.product}</b><br>
                        📞 ${o.customerPhone}<br>
                        📍 ${o.address}<br><br>
                        <button onclick="markAsDone('${doc.id}')">✅ ပို့ဆောင်ပြီး</button>
                    </div>`;
                }
            });
            document.getElementById('list').innerHTML = html || "အော်ဒါအသစ်မရှိပါ။";
        });

        function markAsDone(id) {
            if(confirm("ပို့ဆောင်ပြီးကြောင်း အတည်ပြုမလား?")) {
                db.collection("orders").doc(id).update({ status: "ပို့ဆောင်ပြီး" });
            }
        }
    </script>
</body>
</html>

